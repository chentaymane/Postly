import LoginForm from '../../../components/LoginForm';

export const metadata = {
  title: 'Sign in — Postly',
  description: 'Sign in to Postly, or create a free account.',
};

export default function LoginPage() {
  return <LoginForm />;
}
