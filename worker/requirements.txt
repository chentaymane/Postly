requests>=2.32
APScheduler>=3.10
piper-tts>=1.2          # offline neural voice; ships the `piper` command
python-dotenv>=1.0      # optional: reads worker/.env
# boto3>=1.34           # only for VIDEO_HOST=s3 (Cloudflare R2 etc.)
